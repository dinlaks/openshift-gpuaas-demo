# ManagedCluster — registers Cluster B into ACM Hub
apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: ${SPOKE_CLUSTER_NAME}
  labels:
    cluster.open-cluster-management.io/clusterset: gpuaas-clusterset
    vendor: OpenShift              # required by MCO placementrule_controller to enable observability
    cloud: Other                   # required by MCO placementrule_controller
    demo/gpu-type: ${GPU_TYPE}
    demo/gpu-count: ${GPU_COUNT}
    demo/role: spoke
    demo/server: ${SPOKE_CLUSTER_NAME}
    environment: gpuaas-demo
spec:
  hubAcceptsClient: true
  leaseDurationSeconds: 60
---
# KlusterletAddonConfig — enables work and addon management on Cluster B
apiVersion: agent.open-cluster-management.io/v1
kind: KlusterletAddonConfig
metadata:
  name: ${SPOKE_CLUSTER_NAME}
  namespace: ${SPOKE_CLUSTER_NAME}
spec:
  applicationManager:
    enabled: false
  certPolicyController:
    enabled: false
  iamPolicyController:
    enabled: false
  policyController:
    enabled: true
  searchCollector:
    enabled: true
  workManager:
    enabled: true
